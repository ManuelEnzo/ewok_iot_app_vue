# EwokIoT - Variabili d'ambiente
# Target del proxy Nitro in dev (localhost o ngrok). NON usare NUXT_PUBLIC_* qui:
# quella variabile verrebbe riusata da Nuxt per apiBase e bypasserebbe il proxy.
API_TARGET=http://localhost:5147

# Solo in produzione: URL pubblico del backend (usato dal client direttamente)
# NUXT_PUBLIC_API_BASE=https://api.ewok-iot.it
